[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

# SPA: todo lo que no sea una función o un asset cae en index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
